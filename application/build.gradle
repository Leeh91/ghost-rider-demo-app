plugins {
    id 'groovy'
}

dependencies {
    implementation project(':common')
}

jar {
    archivesBaseName = "${project.name}"
    enabled = true
}

bootJar { enabled = false}
repositories {
    mavenCentral()
}
